# 🧠 Ingals

Многопользовательская игра для изучения английского языка, где игроки соревнуются в правильном переводе слов. Интегрирована в экосистему **Chuvala**.

## 🚀 Возможности

- **Многопользовательская игра**: Найдите соперника и играйте в реальном времени
- **Chuvala SSO**: Единая система входа через Chuvala ID
- **Микросервисная архитектура**: Разделенный Backend и Frontend
- **WebSocket**: Быстрая и отзывчивая игра без перезагрузки страницы
- **Адаптивный дизайн**: Красивый интерфейс на Tailwind CSS

## 🛠️ Технологии

- **Backend**: Python, Flask, Flask-SocketIO (в папке `backend/`)
- **Frontend**: Nginx, Static HTML/JS, Tailwind CSS (в папке `frontend/`)
- **Infrastructure**: Docker, Docker Compose
- **Gateway**: Интеграция с внешним Nginx-шлюзом

## 📦 Установка и запуск

### Предварительные требования
- Docker и Docker Compose
- Запущенный сервис **Chuvala** (SSO провайдер) локально или доступ к продакшену
- Запущенный **Gateway** (опционально, для интеграции по домену)

### Локальный запуск (Docker)

1. **Клонируйте репозиторий**
   ```bash
   git clone git@github.com:azamuray/ingals.git
   cd ingals
   ```

2. **Настройте переменные окружения**
   Создайте файл `.env` (если нет):
   ```bash
   cp .env.example .env # или используйте существующий
   ```
   Убедитесь, что `SSO_LOGIN_URL` указывает на правильный адрес Chuvala (например, `http://localhost:8001/login`).

3. **Запустите проект**
   ```bash
   docker-compose up -d --build
   ```

4. **Откройте браузер**
   Перейдите по адресу: [http://localhost:5002](http://localhost:5002)

## 📁 Структура проекта

```
ingals/
├── backend/               # Flask Application (API & SocketIO)
│   ├── app.py            # Логика игры и API
│   ├── words.json        # База слов
│   ├── requirements.txt  # Python зависимости
│   └── Dockerfile        # Образ бэкенда
├── frontend/              # Static Frontend & Proxy
│   ├── index.html        # SPA-like страница игры
│   ├── nginx.conf        # Конфиг Nginx (проксирование /api)
│   └── Dockerfile        # Образ фронтенда (Nginx)
├── .env                  # Переменные окружения
├── docker-compose.yml    # Описание сервисов
├── CHANGELOG.md          # История изменений
└── README.md             # Документация
```

## 🔧 Конфигурация (.env)

Основные переменные:

- `SSO_LOGIN_URL`: URL для редиректа на страницу входа Chuvala.
- `JWT_SECRET_KEY`: Секретный ключ для валидации токенов (должен совпадать с Chuvala).
- `FORCE_HTTPS`: `true` если проект за прокси с SSL (продакшен), `false` для локальной разработки.

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку (`git checkout -b feature/amazing-feature`)
3. Зафиксируйте изменения (`git commit -m 'Add amazing feature'`)
4. Отправьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📄 Лицензия

MIT License.
